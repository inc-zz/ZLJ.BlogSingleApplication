# 菜单结构与功能模块说明

## 菜单结构

根据设计图，系统采用以下菜单结构：

### 1. 仪表盘
- **路径**: `/dashboard`
- **图标**: Odometer
- **功能**: 数据统计展示

### 2. 用户管理
- **父级路径**: `/user-management`
- **图标**: User
- **子菜单**:
  - **用户列表** (`/user-management/user-list`) ✅ 已实现
    - 功能：查看、搜索、编辑、删除用户
    - 支持批量删除
    - 用户状态管理（启用/禁用）
    - 角色筛选

### 3. 权限管理
- **父级路径**: `/permission`
- **图标**: Lock
- **子菜单**:
  - **管理员列表** (`/permission/admin-list`) 🔲 占位页面
  - **部门管理** (`/permission/department`) 🔲 占位页面
  - **菜单管理** (`/permission/menu`) 🔲 占位页面
  - **角色管理** (`/permission/role`) 🔲 占位页面
  - **角色授权** (`/permission/role-permission`) 🔲 占位页面

### 4. 内容管理
- **父级路径**: `/content`
- **图标**: Document
- **子菜单**:
  - **文章管理** (`/content/articles`) ✅ 已实现
  - **写文章** (`/content/articles/create`) ✅ 已实现
  - **分类管理** (`/content/categories`) ✅ 已实现
  - **标签管理** (`/content/tags`) ✅ 已实现
  - **评论管理** (`/content/comments`) ✅ 已实现

### 5. 系统设置
- **父级路径**: `/system`
- **图标**: Setting
- **子菜单**:
  - **基本设置** (`/system/basic`) 🔲 占位页面
  - **SEO设置** (`/system/seo`) 🔲 占位页面
  - **邮件设置** (`/system/email`) 🔲 占位页面
  - **存储设置** (`/system/storage`) 🔲 占位页面

### 6. 数据统计
- **父级路径**: `/statistics`
- **图标**: DataAnalysis
- **子菜单**:
  - **数据概览** (`/statistics/overview`) 🔲 占位页面
  - **文章统计** (`/statistics/article`) 🔲 占位页面
  - **用户统计** (`/statistics/user`) 🔲 占位页面
  - **访问统计** (`/statistics/visit`) 🔲 占位页面

## 核心功能实现

### 1. 侧边栏菜单 (Sidebar.vue)
- ✅ 支持多级菜单展开/折叠
- ✅ 自动高亮当前激活菜单
- ✅ 图标显示
- ✅ 菜单折叠状态

**特性**:
```vue
<!-- 支持嵌套子菜单 -->
<el-sub-menu v-if="route.children && route.children.length > 0">
  <template #title>
    <el-icon><component :is="route.meta.icon" /></el-icon>
    <span>{{ route.meta?.title }}</span>
  </template>
  <!-- 子菜单项 -->
</el-sub-menu>
```

### 2. 顶部导航 (Header.vue)
- ✅ 面包屑导航
- ✅ 面包屑标签可关闭
- ✅ 用户信息下拉菜单
- ✅ 菜单折叠按钮

**面包屑功能**:
```typescript
// 动态生成面包屑
const breadcrumbs = computed(() => {
  const matched = route.matched.filter((item) => item.meta && item.meta.title)
  // 构建面包屑数组
  return crumbs
})
```

### 3. 用户列表页面 (user-list.vue)
完整的用户管理功能页面：

#### 搜索功能
- 关键词搜索（用户名/姓名/邮箱）
- 角色筛选
- 状态筛选（启用/禁用）

#### 表格功能
- 多选功能
- 分页
- 排序
- 数据加载状态

#### 操作功能
- **查看**: 只读模式查看用户详情
- **编辑**: 编辑用户信息
- **删除**: 单个删除
- **批量删除**: 多选删除
- **添加用户**: 创建新用户

#### 表单验证
```typescript
const formRules: FormRules = {
  userName: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  phoneNumber: [
    { required: true, message: '请输入手机号', trigger: 'blur' },
    { pattern: /^1[3-9]\d{9}$/, message: '手机号格式不正确', trigger: 'blur' },
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '邮箱格式不正确', trigger: 'blur' },
  ],
}
```

## 路由配置

### 路由守卫
```typescript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const title = to.meta.title as string
  
  // 设置页面标题
  if (title) {
    document.title = `${title} - ${import.meta.env.VITE_APP_TITLE}`
  }

  // 检查是否需要登录
  if (to.meta.requiresAuth) {
    if (userStore.token) {
      next()
    } else {
      next('/login')
    }
  } else {
    // 已登录用户访问登录页，重定向到首页
    if (to.path === '/login' && userStore.token) {
      next('/')
    } else {
      next()
    }
  }
})
```

### 路由元信息
```typescript
meta: {
  title: '页面标题',       // 显示在面包屑和浏览器标题
  icon: 'IconName',       // 菜单图标
  requiresAuth: true,     // 是否需要登录
  hidden: false,          // 是否在菜单中隐藏
}
```

## 文件结构

```
src/
├── layouts/
│   ├── MainLayout.vue     # 主布局（已优化）
│   ├── Header.vue         # 顶部导航（已优化）✅
│   └── Sidebar.vue        # 侧边栏菜单（已优化）✅
│
├── views/
│   ├── user-management/
│   │   └── user-list.vue  # 用户列表（完整实现）✅
│   │
│   ├── permission/
│   │   ├── admin-list.vue      # 管理员列表（占位）
│   │   ├── department.vue      # 部门管理（占位）
│   │   ├── menu.vue           # 菜单管理（占位）
│   │   ├── role.vue           # 角色管理（占位）
│   │   └── role-permission.vue # 角色授权（占位）
│   │
│   ├── content/
│   │   ├── articles/      # 文章管理（已有）
│   │   ├── categories/    # 分类管理（已有）
│   │   ├── tags/         # 标签管理（已有）
│   │   └── comments/     # 评论管理（已有）
│   │
│   ├── system/
│   │   ├── basic.vue     # 基本设置（占位）
│   │   ├── seo.vue       # SEO设置（占位）
│   │   ├── email.vue     # 邮件设置（占位）
│   │   └── storage.vue   # 存储设置（占位）
│   │
│   └── statistics/
│       ├── overview.vue  # 数据概览（占位）
│       ├── article.vue   # 文章统计（占位）
│       ├── user.vue      # 用户统计（占位）
│       └── visit.vue     # 访问统计（占位）
│
└── router/
    └── index.ts          # 路由配置（已更新）✅
```

## 待实现功能

### 优先级高
1. **管理员列表页面** - 管理后台管理员账号
2. **部门管理页面** - 组织架构管理（树形结构）
3. **角色管理页面** - 系统角色配置
4. **菜单管理页面** - 动态菜单权限配置

### 优先级中
1. **基本设置页面** - 系统基础配置
2. **SEO设置页面** - 网站SEO优化
3. **数据概览页面** - 统计数据展示

### 优先级低
1. **邮件设置** - 邮件服务器配置
2. **存储设置** - 文件存储配置
3. **各类统计页面** - 详细数据统计

## 开发注意事项

### 1. 路由命名规范
- 使用 PascalCase 命名路由名称
- 路径使用 kebab-case
- 子路由路径不要以 `/` 开头

### 2. 菜单图标
使用 Element Plus Icons：
- `Odometer` - 仪表盘
- `User` - 用户相关
- `Lock` - 权限相关
- `Document` - 文档/内容
- `Setting` - 设置
- `DataAnalysis` - 数据统计

### 3. 页面开发模板
```vue
<template>
  <div class="page-name">
    <el-card>
      <!-- 页面内容 -->
    </el-card>
  </div>
</template>

<script setup lang="ts">
// 页面逻辑
</script>

<style scoped lang="scss">
.page-name {
  // 页面样式
}
</style>
```

### 4. API 集成
所有页面需要：
1. 创建对应的 API 文件（如 `src/api/user.ts`）
2. 定义类型接口（在 `src/types/index.ts`）
3. 使用 `request` 工具进行 HTTP 请求

## 测试访问

启动项目后，可以访问以下页面：
- 仪表盘: http://localhost:3000/dashboard
- 用户列表: http://localhost:3000/user-management/user-list
- 管理员列表: http://localhost:3000/permission/admin-list
- 文章管理: http://localhost:3000/content/articles

## 下一步计划

1. 等待其他模块的设计图
2. 实现权限管理模块的详细功能
3. 对接后端 API
4. 完善数据统计功能
